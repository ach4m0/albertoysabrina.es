@import '../core/parameters.styl'

html, body
    background: $bodybackgroundcolor
    color: $bodycolor
    font-family: $bodyfontfamily
    font-size: $bodyfontsize
    min-height: 100%
    width: 100%

@import '../components/*'
